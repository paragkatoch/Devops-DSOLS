apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-controller
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app: product-controller
  template:
    metadata:
      labels:
        app: product-controller
    spec:
      initContainers:
        - name: wait-for-product-postgres
          image: busybox:1.36
          command:
            [
              "sh",
              "-c",
              "until nc -z product-postgres 5432; do echo waiting for product-postgres; sleep 2; done;",
            ]
        - name: wait-for-rabbitmq
          image: busybox:1.36
          command:
            ["sh", "-c", "until nc -z rabbitmq 5672; do echo waiting for rabbitmq; sleep 2; done;"]
      containers:
        - name: product-controller
          image: devops-dsols-app:latest
          imagePullPolicy: IfNotPresent
          command: ["./app", "-type=controller", "-component=product", "-config=config/local.yml"]
          ports:
            - containerPort: 9000
          volumeMounts:
            - name: app-config-volume
              mountPath: /app/config/local.yml
              subPath: local.yml
          resources:
            requests:
              cpu: "10m"
              memory: "256Mi"
            limits:
              cpu: "50m"
              memory: "256Mi"
      volumes:
        - name: app-config-volume
          secret:
            secretName: app-config
---
apiVersion: v1
kind: Service
metadata:
  name: product-controller
spec:
  selector:
    app: product-controller
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      initContainers:
        - name: wait-for-product-postgres
          image: busybox:1.36
          command:
            [
              "sh",
              "-c",
              "until nc -z product-postgres 5432; do echo waiting for product-postgres; sleep 2; done;",
            ]
        - name: wait-for-rabbitmq
          image: busybox:1.36
          command:
            ["sh", "-c", "until nc -z rabbitmq 5672; do echo waiting for rabbitmq; sleep 2; done;"]
      containers:
        - name: product-service
          image: devops-dsols-app:latest
          imagePullPolicy: IfNotPresent
          command: ["./app", "-type=service", "-component=product", "-config=config/local.yml"]
          ports:
            - containerPort: 9000
          volumeMounts:
            - name: app-config-volume
              mountPath: /app/config/local.yml
              subPath: local.yml
          resources:
            requests:
              cpu: "10m"
              memory: "256Mi"
            limits:
              cpu: "50m"
              memory: "256Mi"
      volumes:
        - name: app-config-volume
          secret:
            secretName: app-config
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
spec:
  selector:
    app: product-service
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
